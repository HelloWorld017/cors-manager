{
  "name": "cors-manager",
  "version": "1.0.1",
  "description": "Manage the policy of CORS for allowed origins",
  "type": "module",
  "scripts": {
    "build": "pnpm run clean && concurrently pnpm:build:*",
    "build:index": "pnpm run build-meta && pnpm run build-userscript",
    "build:library": "esbuild src/library.ts --outfile=dist/library.js",
    "build:types": "tsc",
    "build-meta": "cat src/index.ts | sed -n '/==UserScript==/,/==\\/UserScript==/p' | sed \"s/%%VERSION%%/$(jq -r '.version' package.json)/\" > dist/cors-manager.meta.js",
    "build-userscript": "esbuild src/index.ts --outfile=dist/cors-manager.user.js --bundle --banner:js=\"$(cat dist/cors-manager.meta.js)\"",
    "clean": "rm -rf dist",
    "prepublishOnly": "pnpm run build"
  },
  "main": "dist/library.js",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/library.d.ts",
      "default": "./dist/library.js"
    }
  },
  "author": "nenw* <khi@nenw.dev>",
  "license": "MIT",
  "packageManager": "pnpm@10.18.3",
  "devDependencies": {
    "@types/tampermonkey": "^5.0.5",
    "concurrently": "^9.2.1",
    "esbuild": "^0.27.3",
    "typescript": "^5.9.3",
    "valibot": "^1.2.0"
  },
  "keywords": [
    "cors",
    "userscript"
  ]
}
